doctype html
html
    head
        title #{title}
    body
        h1 Hello from Pug!
        h2 #{text}
        p
            |This is a paragraph with multiples lines
            |Line 2
            |Line 3
        p.
            this is also 
            a multiline p
        form.sign-in(action="submit", method="GET")
            input#input1(type="text", name ="uname")
            input#input2(type="password", name ="password")
            button Submit
        if (user)
            h1  welcome #{user.username}
        else
            form.sign-in(action="login", method="POST")
                input(type="text", name="username")
                br
                input(type="password", name="password")
                br
                button Sign in!
                     
    